# No secrets - example only
# If you want to enable a live model call, set OPENAI_API_KEY here
# No secrets - example only
# To enable live model calls locally, create a private `.env` file (DO NOT commit it)
# Example (in your `.env`, not committed):
# OPENAI_API_KEY=sk-...

OPENAI_API_KEY=

# IMPORTANT: Never commit real API keys. If you accidentally committed one, revoke/rotate it immediately.
